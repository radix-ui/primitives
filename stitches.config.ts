import { createStitches } from '@stitches/core';

export const { css, keyframes } = createStitches({
  theme: {
    colors: {
      white: '#fff',
      gray100: '#ccc',
      gray300: '#aaa',
      black: '#111',
      red: 'crimson',
      green: 'green',
    },
  },
});
