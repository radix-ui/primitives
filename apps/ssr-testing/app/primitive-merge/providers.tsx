'use client';

import * as React from 'react';

export function Providers({ children }: { children: React.ReactNode }) {
  // TODO: Switch with Slot.Provider once merged back into `main`
  return <>{children}</>;
}
