export default ({ frontMatter, ...props }) => <React.Fragment {...props} />;

The reading mode of the document where the slider is rendered. If omitted, `Slider` assumes LTR (left-to-right) reading mode.
