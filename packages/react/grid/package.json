{
  "name": "@interop-ui/react-grid",
  "version": "0.0.1-0",
  "license": "MIT",
  "source": "src/index.ts",
  "main": "dist/index.js",
  "module": "dist/index.module.js",
  "types": "dist/index.d.ts",
  "files": [
    "dist"
  ],
  "scripts": {
    "build": "parcel build src/index.ts && yarn build:styles",
    "build:styles": "node ../../../scripts/build-styles grid",
    "clean": "rm -rf dist",
    "bump": "yarn version --deferred",
    "prepublish": "yarn clean && yarn build"
  },
  "dependencies": {
    "@interop-ui/react-utils": "workspace:*",
    "@interop-ui/utils": "workspace:*"
  },
  "devDependencies": {
    "parcel": "^2.0.0-beta.1"
  },
  "peerDependencies": {
    "react": ">=16"
  }
}
